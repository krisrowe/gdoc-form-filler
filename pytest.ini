[pytest]
# Default to running only unit tests (no network I/O, no credentials needed)
# Use `make test-integration` or `pytest tests/integration/` for integration tests
testpaths = tests/unit

# Verbose output by default
addopts = -v
